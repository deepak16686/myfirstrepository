{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "description": "Live documentation for FastAPI, Python, Docker, Kubernetes, Terraform, and all libraries"
    },
    "postgres": {
      "command": "npx",
      "args": ["-y", "@anthropic-ai/mcp-server-postgres"],
      "env": {
        "DATABASE_URL": "postgresql://postgres:postgres@localhost:5432/postgres"
      },
      "description": "Direct PostgreSQL queries — main DB (ai-postgres), SonarQube DB (ai-sonar-db), Jira DB, Redmine DB"
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@anthropic-ai/mcp-server-memory"],
      "description": "Persistent cross-session memory for pipeline patterns, debug findings, and architectural decisions"
    }
  }
}
