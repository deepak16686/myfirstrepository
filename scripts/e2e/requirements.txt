# ------------------------------------------------------------------------------
# scripts/e2e/requirements.txt
# ------------------------------------------------------------------------------
# Isolated requirements for the E2E smoke + login harness. Do NOT pull these
# into devops-tools-backend/requirements.txt — the backend has its own pinned
# dependency tree and should not carry Playwright.
#
# After `pip install -r requirements.txt` run:
#     playwright install chromium
# to download the Chromium bundle used by tool_login.py.
# ------------------------------------------------------------------------------

# HTTP client used by smoke.py and vault_smoke.py. 0.27+ brings the native
# async connection-pool fixes we rely on.
httpx>=0.27,<1.0

# YAML parsing for tools.yaml. Version-pinned upper bound to avoid
# surprise API changes.
PyYAML>=6.0,<7.0

# Playwright async API (Chromium). Keep upper bound generous — Playwright
# is usually ABI-stable across minors within a year.
playwright>=1.44,<2.0
