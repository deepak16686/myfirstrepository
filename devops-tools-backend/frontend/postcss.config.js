// Tailwind CSS 4 uses the @tailwindcss/vite plugin; this file exists only so legacy
// tooling (IDE PostCSS parsers) has a config to point at. It is intentionally minimal.
export default {
  plugins: {
    autoprefixer: {},
  },
};
